// Open New Vegas — texture cache tests. STUB — implemented by the textures
// work stream (synthetic DDS in a synthetic BSA, asserting decode + caching).
#include "../render/texture_cache.hpp"
#include <cstdio>

int main() {
    std::printf("texture_cache_tests: stub (no assertions yet)\n");
    return 0;
}
