// Open New Vegas — parser robustness tests.
// STUB — implemented by the robustness work stream: feeds truncated,
// corrupted, and adversarial inputs to the BSA/ESM/NIF/DDS parsers and
// asserts they throw clean exceptions instead of crashing or hanging.
#include <cstdio>

int main() {
    std::printf("robustness_tests: stub (no assertions yet)\n");
    return 0;
}
