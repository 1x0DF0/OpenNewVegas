// Open New Vegas — end-to-end integration test.
// STUB — implemented by the integration work stream: builds a complete
// synthetic mini-install (ESM + BSA containing NIF/DDS) and verifies the
// whole pipeline: VFS -> world load -> streamer -> model load -> textures.
#include <cstdio>

int main() {
    std::printf("integration_tests: stub (no assertions yet)\n");
    return 0;
}
